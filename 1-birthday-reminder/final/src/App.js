import React, { useState } from 'react';

function App() {
  const [people, setPeople] = useState(data);
  return (
    <main>
      <section>
        <button >clear all</button>
      </section>
    </main>
  );
}

export default App;
